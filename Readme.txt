Please Uncomment each block at a time to view view each method.

If no comment is uncommented then no Data Imputation occurs.

When one block is uncommented without any other alteration the corresponding data imputation method is used.